/* Vainstein K 2025mar21 */
#include <assert.h>
#include <stdio.h>
#include <string.h>
/* Re why "x &= (x-1)" deletes Rmost 1-bit:
	o	If originally x > 0, then obvious.
	o	If originally x = 0, then there were no 1-bits to delete.
	o	If originally x < 0, also obvious.
*/

#if 0
?????????????????













#endif
